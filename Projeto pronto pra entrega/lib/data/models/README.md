# data/models

Camada para modelos de dados.

Modelos atuais do Muscleway:

- `UsuarioModel`;
- `ProdutoModel`;
- `CarrinhoItemModel`;
- `ChatMensagemModel`.

Eles representam as entidades usadas nas telas, no banco local e no carrinho.
